{
  "rewrites": [
    {
      "source": "/EduCraftA/:id",
      "destination": "/api/redirect?id=:id"
    },
    {
      "source": "/a/:id",
      "destination": "/api/redirect?id=:id"
    }
  ],
  "buildCommand": "npm run build",
  "outputDirectory": "dist"
}
